---
export interface Props {
  name: string;
  role: string;
  image: string;
  quote?: string;
  bioLong: string;
  github?: string;
  scholar?: string;
  website?: string;
  linkedin?: string;
  email: string;
  slug: string;
}

const { name, role, image, quote, bioLong, github, scholar, website, linkedin, email, slug } = Astro.props;
const basePath = import.meta.env.BASE_URL;
const baseUrl = basePath.endsWith('/') ? basePath : basePath + '/';

// Generate initials for fallback
const initials = name
  .split(' ')
  .filter(part => !['Dr.', 'Dr', 'Mr.', 'Mr', 'Ms.', 'Ms', 'Mrs.', 'Mrs', 'Miss', 'Prof.', 'Prof'].includes(part))
  .map(part => part[0])
  .join('')
  .toUpperCase()
  .slice(0, 2);

// Generate unique ID for this card
const cardId = `bio-${slug}`;
---

<div class="card group">
  <div class="flex flex-col items-center text-center">
    <div class="relative mb-4">
      <!-- Image with fallback -->
      <div class="w-40 h-40 rounded-full overflow-hidden mx-auto relative">
        <img 
          src={`${baseUrl}${image.replace(/^\//, '')}`} 
          alt={`${name} profile photo`}
          class="w-full h-full object-cover team-member-img"
          onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"
        />
        <!-- Initials fallback -->
        <div 
          class="absolute inset-0 bg-gradient-to-br from-tekhelet to-skyMagenta flex items-center justify-center text-white text-2xl font-bold"
          style="display: none;"
        >
          {initials}
        </div>
      </div>
    </div>
    
    <h3 class="text-lg font-semibold text-gray-900 mb-1">
      {name}
    </h3>
    
    <p class="text-sm text-tekhelet font-medium mb-3">
      {role}
    </p>
    
    {quote && (
      <blockquote class="text-sm text-gray-600 italic mb-4">
        "{quote}"
      </blockquote>
    )}
    
    <!-- Expandable bio section -->
    <div class="w-full">
      <div 
        id={cardId}
        class="text-sm text-gray-600 leading-relaxed mb-2 overflow-hidden transition-all duration-300 ease-in-out"
        style="max-height: 4.5rem;"
      >
        {bioLong}
      </div>
      <button 
        class="bio-toggle text-xs text-tekhelet hover:text-skyMagenta font-medium flex items-center justify-center mx-auto gap-1 transition-colors duration-200"
        data-target={cardId}
        aria-expanded="false"
      >
        <span class="toggle-text">Read more</span>
        <svg class="w-4 h-4 transform transition-transform duration-300 toggle-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
        </svg>
      </button>
    </div>
    
    <div class="flex space-x-3 mt-4">
      {github && (
        <a 
          href={github} 
          target="_blank" 
          rel="noopener noreferrer"
          class="text-gray-600 hover:text-tekhelet transition-colors duration-200"
          aria-label={`${name} GitHub`}
        >
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
          </svg>
        </a>
      )}
      
      {scholar && (
        <a 
          href={scholar} 
          target="_blank" 
          rel="noopener noreferrer"
          class="text-gray-600 hover:text-tekhelet transition-colors duration-200"
          aria-label={`${name} Google Scholar`}
        >
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
            <path d="M5.242 13.769L0 9.5 12 0l12 9.5-5.242 4.269C17.548 11.249 14.978 9.5 12 9.5c-2.977 0-5.548 1.748-6.758 4.269zM12 10a7 7 0 1 0 0 14 7 7 0 0 0 0-14z"/>
          </svg>
        </a>
      )}
      
      {website && (
        <a 
          href={website} 
          target="_blank" 
          rel="noopener noreferrer"
          class="text-gray-600 hover:text-tekhelet transition-colors duration-200"
          aria-label={`${name} website`}
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/>
          </svg>
        </a>
      )}
      
      {linkedin && (
        <a 
          href={linkedin} 
          target="_blank" 
          rel="noopener noreferrer"
          class="text-gray-600 hover:text-tekhelet transition-colors duration-200"
          aria-label={`${name} LinkedIn`}
        >
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
            <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
          </svg>
        </a>
      )}
      
      <a 
        href={`mailto:${email}`}
        class="text-gray-600 hover:text-tekhelet transition-colors duration-200"
        aria-label={`Email ${name}`}
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
        </svg>
      </a>
    </div>
  </div>
</div>

<script>
  // Bio expand/collapse functionality
  document.querySelectorAll('.bio-toggle').forEach(button => {
    button.addEventListener('click', () => {
      const targetId = button.getAttribute('data-target');
      if (!targetId) return;
      const bioDiv = document.getElementById(targetId) as HTMLElement | null;
      const toggleText = button.querySelector('.toggle-text') as HTMLElement | null;
      const toggleIcon = button.querySelector('.toggle-icon') as HTMLElement | null;
      const isExpanded = button.getAttribute('aria-expanded') === 'true';
      
      if (!bioDiv || !toggleText || !toggleIcon) return;

      if (isExpanded) {
        // Collapse
        bioDiv.style.maxHeight = '4.5rem';
        toggleText.textContent = 'Read more';
        toggleIcon.style.transform = 'rotate(0deg)';
        button.setAttribute('aria-expanded', 'false');
      } else {
        // Expand
        bioDiv.style.maxHeight = bioDiv.scrollHeight + 'px';
        toggleText.textContent = 'Show less';
        toggleIcon.style.transform = 'rotate(180deg)';
        button.setAttribute('aria-expanded', 'true');
      }
    });
  });
</script>
